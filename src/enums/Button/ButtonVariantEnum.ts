/**
 * 按鈕樣式列舉
 * @enum {string}
 * @example
 * import { ButtonVariantEnum } from "enums/Button/ButtonVariantEnum";
 * console.log(ButtonVariantEnum.Contained);
 */
export enum ButtonVariantEnum {
  Contained = "contained",
  Outlined = "outlined",
  Text = "text",
}
